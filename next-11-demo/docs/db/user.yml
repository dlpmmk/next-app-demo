type: object
description: 用户表
properties:
  userid:
    type: string # text
    format: # byte binary date date-time password
    description: |
      用户名
    example:
  name:
    type: string # text
    format: # byte binary date date-time password
    description: |
      姓名
    example:
  sex:
    type: integer # smallint
    format: int32
    description: |
      性别：0 女 1 男
    example:
  password:
    type: string # text
    format: # byte binary date date-time password
    description: |
      密码
    example:
  phone:
    type: string # text
    format: # byte binary date date-time password
    description: |
      手机号
    example:
  email:
    type: string # text
    format: # byte binary date date-time password
    description: |
      邮箱
    example:
  avatar:
    type: string # text
    format: # byte binary date date-time password
    description: |
      用户头像
    example:
  state:
    type: integer # smallint
    format: int32
    description: |
      逻辑删除标识：0 删除 1 在用
    example:
  update_name:
    type: string # text
    format: # byte binary date date-time password
    description: |
      更新人姓名
    example:
  update_time:
    type: integer # bigint
    format: int64
    description: |
      更新时间
    example:
